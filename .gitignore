# CTIBench data is CC BY-NC-SA (NonCommercial) — do NOT redistribute; download at runtime.
data/cti-vsp.tsv
data/cti-*.tsv

# secrets — never commit API keys
*.key
.env
.env.*
secrets/

# run outputs / results
results/
runs/
*.attestation.cdx.json
out*.json

# python
__pycache__/
*.pyc
.venv/

# compiled vens binary (rebuild from the vens repo) and downloaded reference papers
vens
*.pdf
# ...but ship our paper's readable PDF (the .tex source is tracked too)
!paper/vens-benchmark.pdf
