project:
  name: svc
  description: service
context:
  exposure: internet
  data_sensitivity: critical
  business_criticality: critical
  notes: The vulnerable code path is compiled out / feature-flagged off in this build;
    it is not reachable at runtime.
